#get two inputs from user
a = int(input("What would the first number be?: "))
b = int(input("What would the second number be?: "))

#print a and b
print("a =", a)
print("b =", b)

#swap the values of each using the following:

#assign a's value to variable temp
temp = a

#change a's value to b's value
a = b

#change b's value to temp's value so that the value is swapped
b = temp

#print a and b
print("changed a =", a)
print("changed b =", b)

